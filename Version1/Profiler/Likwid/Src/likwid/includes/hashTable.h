#include <bstrlib.h>
#include <types.h>

extern LikwidThreadResults* hashTable_get(bstring regionTag);
extern void hashTable_finalize(int* numberOfThreads, int* numberOfRegions, LikwidResults** results);


